Dim peso As Double
Dim agua as Double
Dim quantidadeIdeal as Double

Input peso
Input agua

quantidadeIdeal = peso * 35

If agua >= quantidadeIdeal Then
    Print "Meta atingida!"
Else
    Print "Meta nao atingida"
End if

Sleep
